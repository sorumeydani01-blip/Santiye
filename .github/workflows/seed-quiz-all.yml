name: Soru Havuzunu Yükle (Tek Seferlik)

on:
  workflow_dispatch:

jobs:
  seed:
    runs-on: ubuntu-latest
    steps:
      - name: Kodu al
        uses: actions/checkout@v4

      - name: Node.js kur
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Bagimliligi kur (firebase-admin)
        run: npm install firebase-admin@12

      - name: 'Paket: seed-quiz-bolum1.js'
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: node scripts/seed-quiz-bolum1.js
        continue-on-error: true

      - name: 'Paket: seed-quiz-bolum2.js'
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: node scripts/seed-quiz-bolum2.js
        continue-on-error: true

      - name: 'Paket: seed-quiz-bolum3.js'
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: node scripts/seed-quiz-bolum3.js
        continue-on-error: true

      - name: 'Paket: seed-quiz-bolum4.js'
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: node scripts/seed-quiz-bolum4.js
        continue-on-error: true

      - name: 'Paket: seed-quiz-bolum5.js'
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: node scripts/seed-quiz-bolum5.js
        continue-on-error: true

      - name: 'Paket: seed-quiz-bolum6.js'
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: node scripts/seed-quiz-bolum6.js
        continue-on-error: true

      - name: 'Paket: seed-quiz-bolum7.js'
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: node scripts/seed-quiz-bolum7.js
        continue-on-error: true

      - name: 'Paket: seed-quiz-bolum8.js'
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: node scripts/seed-quiz-bolum8.js
        continue-on-error: true

      - name: 'Paket: seed-quiz-bolum9.js'
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: node scripts/seed-quiz-bolum9.js
        continue-on-error: true

      - name: 'Paket: seed-quiz-bolum10.js'
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: node scripts/seed-quiz-bolum10.js
        continue-on-error: true

      - name: 'Paket: seed-quiz-bolum11.js'
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: node scripts/seed-quiz-bolum11.js
        continue-on-error: true

      - name: 'Paket: seed-quiz-bolum12.js'
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: node scripts/seed-quiz-bolum12.js
        continue-on-error: true

      - name: 'Paket: seed-quiz-bolum13.js'
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: node scripts/seed-quiz-bolum13.js
        continue-on-error: true

      - name: 'Paket: seed-quiz-bolum14.js'
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: node scripts/seed-quiz-bolum14.js
        continue-on-error: true

      - name: 'Paket: seed-quiz-bolum15.js'
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: node scripts/seed-quiz-bolum15.js
        continue-on-error: true

      - name: 'Paket: seed-quiz-bolum16.js'
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: node scripts/seed-quiz-bolum16.js
        continue-on-error: true

      - name: 'Paket: seed-quiz-bolum17.js'
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: node scripts/seed-quiz-bolum17.js
        continue-on-error: true

      - name: 'Paket: seed-quiz-bolum18.js'
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: node scripts/seed-quiz-bolum18.js
        continue-on-error: true

      - name: 'Paket: seed-quiz-bolum19.js'
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: node scripts/seed-quiz-bolum19.js
        continue-on-error: true

      - name: 'Paket: seed-quiz-bolum20.js'
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: node scripts/seed-quiz-bolum20.js
        continue-on-error: true

      - name: 'Paket: seed-quiz-bolum21.js'
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: node scripts/seed-quiz-bolum21.js
        continue-on-error: true

      - name: 'Paket: seed-quiz-bolum22.js'
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: node scripts/seed-quiz-bolum22.js
        continue-on-error: true
